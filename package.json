{
  "name": "audio-bot-cloudflare",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
